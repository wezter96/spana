[install]
# Only install package versions published at least 2 days ago (supply chain protection)
minimumReleaseAge = 172800 # 2 days in seconds

# Trusted packages that can bypass the age gate
minimumReleaseAgeExcludes = ["@types/bun", "@types/node", "typescript"]
